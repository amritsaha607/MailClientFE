import { RenderMailContentPipe } from './render-mail-content.pipe';

describe('RenderMailContentPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderMailContentPipe();
    expect(pipe).toBeTruthy();
  });
});
